import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#40231b',
      contrastText: '#fff',
    },
    secondary: {
      main: '#768938',
      contrastText: '#FFF',
    },
  },
});

export default theme;